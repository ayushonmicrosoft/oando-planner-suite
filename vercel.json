{
  "buildCommand": "pnpm --filter @workspace/planner-suite run build",
  "outputDirectory": "artifacts/planner-suite/build",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
