{
  "buildCommand": "pnpm -w run vercel-build",
  "outputDirectory": "artifacts/planner-suite/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
