{
  "buildCommand": "pnpm --filter @workspace/planner-suite run build && cp -r artifacts/planner-suite/build output",
  "outputDirectory": "output",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
